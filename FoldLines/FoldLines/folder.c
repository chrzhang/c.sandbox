#include <stdio.h>

/*
(Exercise 1-22)
Fold long input lines into 2 or more shorter lines after the last non-blank char
that occurs.
*/
int main() {
	return 0;
}

// Initialize a counter that will RESET everytime a newline char is encountered
// If it reaches the given column-width
	// Find the last char that is not a blank
		// Insert a newline character after it
		// Follow it with an escape \0 character
		